- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.screenrc:
    ~/.vimrc:
    ~/.zshrc:
    ~/.zshenv:

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

- shell:
   - command: ./setup_homebrew.zsh
     stdout: true
     stderr: true
