- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.screenrc:
    ~/.vimrc:
    ~/.zshrc:

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

- shell:
   - command: ./setup_homebrew.zsh
     stdout: true
     stderr: true
