- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.screenrc:
    ~/.vimrc:
    ~/.zshrc:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
#    ~/.vimrc: vimrc

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
